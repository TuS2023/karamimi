<div class="container">
  <div class="row">
    <div>
      <%= link_to image_tag( review.image.variant(resize: '100x100') ), review_path(review.id) %>
      <%= link_to review.user.name, user_path(review.user.id) %>
      <i class="fa fa-heart unlike-btn"></i>
      <%= review.likes.length %>
      <%= link_to review.title, review_path(review.id) %>
    </div>
  </div>
</div>

