<%= form_with model: [review, review_comment] do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: "コメントをここに", class: "form-control w-100" %>
  <%= f.submit "送信", class: "btn btn-primary" %>
<% end %>